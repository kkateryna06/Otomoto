DATABASE_URL = "postgres://{user}:{password}@{hostname}:{port}/{database_name}"

DB_SETTINGS = {
    "host": "localhost",
    "database": "database_name",
    "user": "user",
    "password": "password",
    "port": "port"
}